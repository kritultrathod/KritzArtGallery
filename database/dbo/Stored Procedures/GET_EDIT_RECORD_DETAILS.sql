CREATE PROCEDURE GET_EDIT_RECORD_DETAILS @id INT
AS
SET NOCOUNT ON

DECLARE @CountRec INT

SELECT @CountRec = COUNT(*)
FROM ArtDetails
WHERE ID = @id

IF @CountRec <> 0
BEGIN

	SELECT *
	FROM ArtDetails
	WHERE ID = @id

	SET ROWCOUNT 0

	PRINT 0

	RETURN 0
END
ELSE
	PRINT 1

RETURN 1
